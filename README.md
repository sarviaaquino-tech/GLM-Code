GLM1_Categories = Bash code for the lab server- It creates beta maps for each of the categories in the task

PONIESconcatenate = MATLAB code- This is to concatenate 7 runs into one file to use in the GLM code for PONIes - "person(variable) of no interest"
